---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: indiehosters/git # any container with git will do
    tag: 'latest'

inputs:
- name: repo

outputs:
- name: cache

run:
  path: sh
  args:
  - -exc
  - |
    set
    ls -laR
    echo "done" > cache/a
